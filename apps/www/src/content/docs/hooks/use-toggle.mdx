---
title: useToggle
description: Docs for useToggle hook.
---

import { Code, Tabs, TabItem } from '@astrojs/starlight/components';
import hookTypescript from "../../../../../../packages/registry/src/hooks/use-toggle/index.ts?raw";
import hookJavascript from "../../../../../../packages/registry/src/hooks/use-toggle/index.js?raw";

Just a simplier way of dealing with boolean states.

### Installation
<Code code="npx rabbithook@latest add use-toggle" lang="bash" frame={false} />

### Usage
<Code
    lang="tsx"
    title="App.tsx"
    frame="code"
    code={`
import useToggle from "@/hooks/use-toggle";

function Component() {
  const [state, toggleState] = useToggle(false);

  return (
    <>
      <p>State: { JSON.stringify(state) }</p>

      <button onClick={() => toggleState()}>Toggle</button>
      <button onClick={() => toggleState(false)}>Set to false</button>
      <button onClick={() => toggleState(true)}>Set to true</button>
    </>
  );
}
`}
  />

### Code
<Tabs>
  <TabItem label="Typescript">
    <Code
      lang="ts"
      title="use-toggle.ts"
      frame="code"
      code={hookTypescript}
    />
  </TabItem>
  <TabItem label="Javascript">
    <Code
      lang="js"
      title="use-toggle.js"
      frame="code"
      code={hookJavascript}
    />
  </TabItem>
</Tabs>

