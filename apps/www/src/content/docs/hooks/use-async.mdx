---
title: useAsync
description: Docs for useAsync hook.
---

import { Code, Tabs, TabItem } from '@astrojs/starlight/components';
import hookTypescript from "../../../../../../packages/registry/src/hooks/use-async/index.ts?raw";
import hookJavascript from "../../../../../../packages/registry/src/hooks/use-async/index.js?raw";

Resolves synchronously any asynchronous function that you have, returning the data itself along with loading and error states.

### Installation

<Code code="npx rabbithook@latest add use-async" lang="bash" frame={false} />

### Usage
<Code
  lang="tsx"
  title="App.tsx"
  frame="code"
  code={`
import useAsync from "@/hooks/use-async";

function Component() {
  const { value, loading, error } = useAsync(() => {
    const response = await fetch("...");

    return response.json()
  });

  return (
    <div>
      {loading && <p>Loading...</p>}
      {error && <p>Error: {error.message}</p>}
      {value && <p>Value: {value}</p>}
    </div>
  )
}
`}
/>

### Code
<Tabs>
  <TabItem label="Typescript">
    <Code
      lang="ts"ß
      title="use-async.ts"
      frame="code"
      code={hookTypescript}
    />
  </TabItem>
  <TabItem label="Javascript">
    <Code
      lang="js"
      title="use-async.js"
      frame="code"
      code={hookJavascript}
    />
  </TabItem>
</Tabs>

