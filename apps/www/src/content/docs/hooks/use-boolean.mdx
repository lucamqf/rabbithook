---
title: useBoolean
description: Docs for useBoolean hook.
---

import { Code, Tabs, TabItem } from '@astrojs/starlight/components';
import hookTypescript from "../../../../../../packages/registry/src/hooks/use-boolean/index.ts?raw";
import hookJavascript from "../../../../../../packages/registry/src/hooks/use-boolean/index.js?raw";

Just a simplier way of dealing with boolean states.

### Installation
<Code code="npx rabbithook@latest add use-boolean" lang="bash" frame={false} />

### Usage
<Code
    lang="tsx"
    title="App.tsx"
    frame="code"
    code={`
import useBoolean from "@/hooks/use-boolean";

function Component() {
  const [state, toggleState] = useBoolean(false);

  return (
    <>
      <p>State: { JSON.stringify(state) }</p>

      <button onClick={() => toggleState()}>Toggle</button>
      <button onClick={() => toggleState(false)}>Set to false</button>
      <button onClick={() => toggleState(true)}>Set to true</button>
    </>
  );
}
`}
  />

### Code
<Tabs>
  <TabItem label="Typescript">
    <Code
      lang="ts"
      title="use-boolean.ts"
      frame="code"
      code={hookTypescript}
    />
  </TabItem>
  <TabItem label="Javascript">
    <Code
      lang="js"
      title="use-boolean.js"
      frame="code"
      code={hookJavascript}
    />
  </TabItem>
</Tabs>

