---
title: useHover
description: Docs for useHover hook.
---

import { Code, Tabs, TabItem } from '@astrojs/starlight/components';
import hookTypescript from "../../../../../../packages/registry/src/hooks/use-hover/index.ts?raw";
import hookJavascript from "../../../../../../packages/registry/src/hooks/use-hover/index.js?raw";

Tracks the hover state of an element.

### Installation
<Code code="npx rabbithook@latest add use-hover" lang="bash" frame={false} />

### Usage
<Tabs>
  <TabItem label="Typescript">
    <Code
    lang="tsx"
    title="App.tsx"
    frame="code"
    code={`
import useHover from "@/hooks/use-hover";

function Component() {
  const ref = useRef<HTMLDivElement>(null);

  const isHovered = useHover(ref);

  return (
    <div ref={ref}>isHovered: { isHovered }</div>
  )
}
`}
  />
  </TabItem>

  <TabItem label="Javascript">
    <Code
    lang="jsx"
    title="App.jsx"
    frame="code"
    code={`
import useHover from "@/hooks/use-hover";

function Component() {
  const ref = useRef();

  const isHovered = useHover(ref);

  return (
    <div ref={ref}>
      isHovered: { isHovered ? "Yes" : "No" }
    </div>
  )
}
`}
  />
  </TabItem>
</Tabs>

### Code
<Tabs>
  <TabItem label="Typescript">
    <Code
      lang="ts"
      title="use-hover.ts"
      frame="code"
      code={hookTypescript}
    />
  </TabItem>
  <TabItem label="Javascript">
    <Code
      lang="js"
      title="use-hover.js"
      frame="code"
      code={hookJavascript}
    />
  </TabItem>
</Tabs>

